const app = require("./app.js");
console.log(`sum function call: ${app.sum()}`);
console.log(`call for x variable: ${app.x}`)
console.log(`call for x variable: ${app.y}`);

/*
// Importing the cube module from app.js
const cube = require("./app.js");

// Creating a new instance of the cube with side length 2
const mySquare = new cube(2);

// Logging the area of mySquare to the console
console.log(`The area of mySquare is ${mySquare.area()}`);
*/